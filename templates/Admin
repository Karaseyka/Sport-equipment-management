<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Учет школьного инвентаря - Администратор</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .header {
            background-color: #3487e3;
            color: white;
            padding: 40px;
            text-align: center;
        }

        .inventory-list {
            margin-top: 20px;
        }

        .inventory-item {
            background-color: #e9ecef;
            border: 1px solid #ced4da;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 10px;
            transition: transform 0.2s;
            cursor: pointer;
        }

        .inventory-item:hover {
            transform: scale(1.05);
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            padding: 10px;
            background-color: #3487e3;
            color: white;
        }

        .up {
            vertical-align: 45px;
        }
    </style>
</head>
<body>
<div class="header">
    <span class="up" style="float:left">Администрирование инвентаря</span>
    <span class="up" style="float:right">{{ name }}</span>
</div>

<div class="container inventory-list">
    <h2 align="center">Управление инвентарем</h2>
    <div class="text-center mb-4">
        <a href="/add-user" class="btn btn-primary">Добавить пользователя</a>
        <a href="/add-inventory-item" class="btn btn-primary">Добавить новый инвентарь</a>
        <a href="/purchase-plan" class="btn btn-primary">План закупок</a>
        <a href="/user-list" class="btn btn-primary">Список пользователей</a>
    </div>

    <h2 align="center">Доступное Вам</h2>
    <div class="row">
        <div class="col-sm-6">
            {% for item in inventory %}
            {% if inventory.index(item) % 2 == 0 %}
            <div class="col-md-14">
                <div class="inventory-item">
                    <h5>{{ item.name }}</h5>
                    <p>Количество предметов: {{ item.count }}</p>
                </div>
            </div>
            {% endif %}
            {% endfor %}
        </div>
        <div class="col-sm-6">
            {% for item in inventory %}
            {% if inventory.index(item) % 2 != 0 %}
            <div class="col-md-14">
                <div class="inventory-item">
                    <h5>{{ item.name }}</h5>
                    <p>Количество предметов: {{ item.count }}</p>
                </div>
            </div>
            {% endif %}
            {% endfor %}
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
